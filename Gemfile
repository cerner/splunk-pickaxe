# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

group :test do
  gem 'bundler', '~> 2.0'
  gem 'rake', '~> 12.0'
  gem 'rspec', '~> 3.5'
end

gem 'pry-rails', :group => :development

group :release do
  gem 'octokit'
end
